"""Benchmarks measuring Visual Verifier against simpler alternatives.

Nothing here ships in the wheel. Benchmarks answer a different question
from tests: a test says the implementation behaves as designed, while a
benchmark says the approach solves a problem simpler methods do not.
"""
