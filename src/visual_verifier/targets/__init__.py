"""Reserve the namespace for target-provider implementations.

Automatic target discovery, reviewed CSV targets, interpolation, and
target-aware validation remain development placeholders. They must not be
treated as supported public APIs until their behavior is implemented,
documented, and covered by regression tests.
"""

__all__: list[str] = []
