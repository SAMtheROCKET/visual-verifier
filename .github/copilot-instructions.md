Read and follow `/AGENTS.md` and `/docs/CODE_STYLE.md` before editing.
Treat `archive/legacy_cells/` as read-only. Active code belongs under
`src/visual_verifier/`. Every behavior change requires tests. Preserve the
three-video regression contract and run all required `uv` quality checks.
